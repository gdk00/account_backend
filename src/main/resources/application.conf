database {
  postgres {
    connectionPool = "HikariCP"
    dataSourceClass = "org.postgresql.ds.PGSimpleDataSource"
    numThreads = 10

    properties = {
      serverName = 0.0.0.0 #${PGHOST}
      portNumber = 5434 #${PGPORT}
      databaseName = "fun-cart"
      user = "postgres"
      password = "1111"
    }
  }
}